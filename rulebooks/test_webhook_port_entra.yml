---
- name: any
  hosts: localhost
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: "{{ webhook_port | default ('5555') }}"
        oauth_jwks_url: "{{ oauth_jwks_url }}"
        oauth_audience: "{{ oauth_audience }}"
  rules:
    - name: r1
      condition: true
      action:
        debug:
