---
- name: Webhook rulebook
  hosts: all
  sources:
    - name: my_github 
      ansible.eda.webhook:
        port: 5555
        host: 0.0.0.0
  rules:
    - name: Active Webhook event
      condition: event.payload.hook.active
      action:
        debug:
